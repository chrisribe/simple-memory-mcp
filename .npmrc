# Force npm to use the correct Node.js runtime
# This helps prevent issues with WSL using Windows Node.js

# Build native modules for the current platform
build-from-source=false

# Publishing defaults
access=public
registry=https://registry.npmjs.org/
